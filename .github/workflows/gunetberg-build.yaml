name: "Gunetberg image"
on:
    push:
        branches: [release]

      
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3 
            - name: Build the Docker image 
              run: docker build . --file Dockerfile.build --tag $DOCKER_USER/$REPO_NAME:test